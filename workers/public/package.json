{
  "name": "@zudo-ez-host/public-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler types src/worker-configuration.d.ts --env-interface PublicEnv --include-runtime false --check && wrangler deploy --dry-run --config wrangler.toml",
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@zudo-ez-host/core": "workspace:*"
  },
  "devDependencies": {
    "wrangler": "^4.127.0"
  }
}
