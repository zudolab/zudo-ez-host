# pnpm 11 reads only auth/registry settings from here — peer-deps policy,
# engine strictness, and the build-script allowlist live in pnpm-workspace.yaml.
#
# Add public-hoist-pattern entries only for deps that resolve by filesystem
# traversal rather than Node resolution, e.g.:
# public-hoist-pattern[]=miniflare
